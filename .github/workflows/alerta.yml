name: Notificación

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar notificación
        run: curl -d "¡Código subido! 🚀 Proyecto Hola Mundo simple" ntfy.sh/devops-itla

